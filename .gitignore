# Dependencies
node_modules/
client/node_modules/

# Build output — both possible locations
client/dist/
client-dist/

# Environment variables — NEVER commit this
.env
.env.*
!.env.example

# Local data — stays on your machine only (repo-root only; NOT server/vault code folder)
/data/
/backups/
/vault/

# Bank scraper HAR fixtures — contain real bank session data / session cookies
server/scraper-fixtures/

# Bank scraper — automation code for live/record/replay bank sessions; keep local only
extension/
client/src/pages/BankScraper/

# Imported Python scrapers (chase/boa/amazon/mortgage) — credential-handling
# automation + scraped financial data (PDFs, SQLite, session profiles). Each is
# its own git repo under the repo-root scrapers/; keep ALL of it out of the CaiShen repo.
/scrapers/
# Server-side scraper automation (bank-scraper, scraper-bridge, scraper-import) —
# local-only, same posture as the Python scrapers. The whole folder stays out of git.
server/scrapers/
client/src/pages/Scrapers/

# Log files
*.log
server_log.txt
logs/

# OS files
.DS_Store
Thumbs.db

# Dropbox
.dropboxignore
excludelist.txt

# Personal working doc — feature roadmap (local only, do not commit)
ROADMAP.md