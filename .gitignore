# Dependencies
node_modules/
client/node_modules/

# Build output — both possible locations
client/dist/
client-dist/

# Environment variables — NEVER commit this
.env
.env.*
!.env.example

# Local data — stays on your machine only
data/
backups/
vault/

# Bank scraper HAR fixtures — contain real bank session data / session cookies
server/scraper-fixtures/

# Bank scraper — automation code for live/record/replay bank sessions; keep local only
extension/
server/bank-scraper.js
client/src/BankScraper.jsx

# Imported Python scrapers (chase/boa/amazon/mortgage) — credential-handling
# automation + scraped financial data (PDFs, SQLite, session profiles). Each is
# its own git repo under here; keep ALL of it out of the CaiShen repo.
scrapers/
# Node/React bridge for those scrapers — local-only, same posture as bank-scraper.js
server/scraper-bridge.js
server/scraper-import.js
client/src/Scrapers.jsx

# Log files
*.log
server_log.txt
logs/

# OS files
.DS_Store
Thumbs.db

# Dropbox
.dropboxignore
excludelist.txt

# Personal working doc — feature roadmap (local only, do not commit)
ROADMAP.md